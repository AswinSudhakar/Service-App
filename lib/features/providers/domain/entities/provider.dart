class Provider {
  final String id;
  final String name;
  final String serviceId;
  final bool isApproved;

  const Provider({
    required this.id,
    required this.name,
    required this.serviceId,
    required this.isApproved,
  });
}
